# Frontend Production Environment Variables
# This file is used when building the frontend for web deployment

# API URL for production
# Use empty string since nginx proxies routes like /auth, /presets directly to backend
VITE_API_URL=
