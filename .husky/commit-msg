echo "🔍 Validating commit message..."

npx --no -- commitlint --edit $1

if [ $? -ne 0 ]; then
  echo "❌ Commit message validation failed."
  echo "💡 Use 'npm run commit' for guided commit creation."
  exit 1
fi

echo "✅ Commit message validation passed!"
