{
  "name": "bendbionics",
  "version": "1.0.0",
  "description": "Soft Robot Simulation Platform",
  "scripts": {
    "prepare": "husky",
    "setup": "./scripts/setup.sh",
    "dev": "./dev.sh",
    "build": "./build.sh",
    "health": "./scripts/health-check.sh",
    "lint": "cd frontend && npm run lint",
    "lint:fix": "cd frontend && npm run lint:fix",
    "lint:check": "cd frontend && npm run lint -- --quiet",
    "test": "cd frontend && npm run test:run",
    "test:core": "cd frontend && npm run test -- --run && cd ../backend && python -m pytest tests/test_core_apis.py -v",
    "test:watch": "cd frontend && npm run test:watch",
    "test:coverage": "cd frontend && npm run test:coverage",
    "commit": "echo 'Use Cursor\\'s Git tab for AI-generated commits or run: npx cz'",
    "changelog": "echo 'Changelog generation available via commitlint ecosystem'",
    "version": "echo 'Version management available via commitlint ecosystem'",
    "quality": "node scripts/code-quality-report.js",
    "quality:frontend": "cd frontend && npm run lint && npx tsc --noEmit",
    "quality:backend": "cd backend && ruff check app/ && python -m mypy app/",
    "ci:test": "npm run test && cd backend && python -m pytest tests/ -v",
    "ci:lint": "npm run lint && cd backend && ruff check app/ && ruff format --check app/",
    "ci:build": "cd frontend && npm run build && cd ../backend && python -c 'import app.main'",
    "ci:all": "npm run ci:lint && npm run ci:test && npm run ci:build",
    "toolkit": "./toolkit.sh",
    "toolkit:test": "./toolkit.sh all test",
    "toolkit:lint": "./toolkit.sh all lint",
    "toolkit:fix": "./toolkit.sh all fix",
    "toolkit:quality": "./toolkit.sh quality report",
    "toolkit:docs": "./toolkit.sh docs storybook",
    "cleanup": "./scripts/cleanup.sh",
    "cleanup:smart": "./scripts/cleanup.sh --smart",
    "cleanup:force": "./scripts/cleanup.sh --force",
    "cleanup:status": "./scripts/cleanup.sh status",
    "cleanup:check": "./scripts/cleanup.sh check",
    "cleanup:schedule": "./scripts/cleanup.sh schedule"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
